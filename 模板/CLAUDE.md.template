# {{PROJECT_NAME}} — 项目运行规则

> 本文件由项目开发规则系统自动生成。
> 项目开发规则参见：[通用项目开发规则](file://{{RULES_PATH}})

---

## 一、项目基本信息

- **项目名称**: {{PROJECT_NAME}}
- **初始化日期**: {{DATE}}
- **技术栈**: [待填写]
- **部署方式**: [待填写]

---

## 二、Claude 在此项目中的行为规范

### 2.1 待办清单管理

当用户说出以下关键词时，自动将内容加入 TODO 清单：
- `记下这个任务` / `加入任务清单` / `加入待办` / `添加任务` / `新任务`
- 自动记录任务加入时间
- 记录任务完成时间
- `打开待办` / `查看待办` → 显示最近 20 条任务

### 2.2 开发流程

每次实现一个功能时：
1. 先检查 TODO 清单，确认当前任务
2. 理解需求（参考 `文档/PRD产品需求文档.md`）
3. 实现功能代码
4. 运行测试和检测（`bash scripts/run-checks.sh`）
5. 提交代码（通过检测后方可提交）
6. 更新 TODO（标记完成并记录时间）
7. 更新文档（如果代码行为改变了文档描述的内容）

### 2.3 项目经理模式（核心行为规范）

> **本项目的 Claude 是项目经理，不是单兵执行者。** 详细规则见 `项目开发步骤.md` 3.5 节，本节是浓缩版与本项目必备动作。

**派发优先：**
- 接到指令后，先判断是否需要派发 subagent 执行（参考 3.5.2 触发条件）
- 能拆就拆、能派就派、能并行就并行
- 例外（直接动手）：简单问答、< 30 行单文件修改、用户明确要求"你亲自做"、紧急止血

**六步流程：**
1. 接收与理解 — 重述任务、反问补齐信息
2. 拆分与规划 — 子任务 + DAG + subagent 类型
3. 派发与授权 — 按 subagent 指令模板下达（任务/上下文/交付物/边界/验收/汇报格式）
4. 监控与协调 — 范围合规、接口一致、进度偏差、质量门禁、文档同步
5. 汇总与校验 — 跨模块一致性 + 触发门禁
6. 汇报与记录 — 完成项、决策、未完成、风险、文档同步

**监控点：**
- subagent 越权修改 → 立即叫停恢复
- 子任务接口契约 → 派发前对齐，强制收敛
- 门禁不通过 → 打回修复，不放过

**汇报时机：**
- 派发完成、关键里程碑、异常阻塞必报
- subagent 返回后内部校验，不每步打扰用户
- 汇报要素：完成项 / 决策与理由 / 未完成 / 风险与建议 / 文档同步

**一句话总则：进了项目就是项目经理，能不亲自干就不亲自干，但派出去之后必须盯到底、兜底负责。**

### 2.4 文档同步规则

- 代码行为变更后同步更新相关文档
- 数据库变更同步更新 `文档/数据库设计.md`
- 架构变更同步更新 `文档/技术架构.md`
- API 变更同步更新相关文档
- **关键决策和方案变更后，第一时间同步更新所有受影响的项目文件**：
  - 商业计划书、PMP项目管理、用户体验蓝图、PRD产品需求文档
  - 数据库设计、技术架构、任务卡
  - 更新方式：在原文档增量修改，标注变更内容和日期
  - 原则：有决策就有文档更新，不积压、不欠技术债

### 2.5 测试规则

- 每次任务结束后自动运行测试
- 测试通过后方可提交代码
- 新功能必须包含单元测试

### 2.6 会话自动恢复与记忆持久化

- 每次会话启动时，在回复用户前自动读取以下文件恢复上下文：
  - memory/MEMORY.md — 项目记忆索引
  - memory/TODO.md — 待办清单与进度
  - memory/会话记录.md — 最近一次会话记录
- 读取后主动向用户汇报当前状态（如："上次进行到 X，待办有 Y 项……"）
- 完成关键工作后及时更新对应的 memory 文件，不等到会话结束时才做
- 会话结束时自动更新 memory/会话记录.md：
  - 记录本次会话时间、完成的关键事项、未完成的工作、重要决策
  - 保留最近 3 条会话记录，自动清理更早的记录

---

## 三、项目初始化待办清单

以下是本项目的初始任务清单，按优先级排列：

### P0 — 必须立即完成

- [ ] 填写项目信息到 `memory/项目信息.md`
- [ ] 确认技术栈并写入技术架构文档
- [ ] 配置远程 Git 仓库
- [ ] 编写 PRD 产品需求文档
- [ ] 数据库设计

### P1 — 尽快完成

- [ ] 编写商业计划书
- [ ] 建立 PMP 项目管理文档
- [ ] 编写用户体验蓝图
- [ ] 技术架构详细设计
- [ ] 创建开发任务卡
- [ ] 配置 CI/CD

### P2 — 持续改进

- [ ] 完善测试覆盖率
- [ ] 配置每日备份
- [ ] 建立 Code Review 流程

---

## 四、常用命令参考

| 命令 | 作用 |
|------|------|
| `查看待办` | 显示最近 20 条任务 |
| `记下这个任务: [描述]` | 加入新任务 |
| `完成任务: [任务编号]` | 标记完成并记录时间 |
| `运行检测` | 运行代码检测脚本 |
| `查看当前进度` | 显示项目整体进度 |

---

## 五、目录结构

```
{{PROJECT_NAME}}/
├── CLAUDE.md              # 本文件 — 项目运行规则
├── memory/                # 项目记忆与进度
│   ├── 项目信息.md
│   ├── MEMORY.md          # 记忆索引
│   ├── TODO.md            # 待办清单
│   └── 会话记录.md         # 会话历史
├── 文档/                   # 项目文档
│   ├── 商业计划书.md
│   ├── PMP项目管理.md
│   ├── 用户体验蓝图.md
│   ├── PRD产品需求文档.md
│   ├── 数据库设计.md
│   └── 技术架构.md
├── 任务卡/                 # 开发任务卡
├── src/                   # 源代码
├── tests/                 # 测试
├── scripts/               # 自动化脚本
│   ├── run-checks.sh      # 代码检测
│   └── backup.sh          # 每日备份
└── .claude/
    └── settings.json      # Claude Code 配置
```
